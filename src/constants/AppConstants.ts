export enum Routes {
    HOME_PAGE = "/",
    HELP_PAGE = "/help",
    ADD_EXPENSE = "/add",
    EDIT_EXPENSE = "/edit"
}
export const editExpensePageExtension = "/:id";

export enum SortBy {
    amount = "amount",
    date = "date"
}
