import React from "react";
import ExpenseList from "./expense/ExpenseList";

const HomePage = () => {
    return (
        <div>
            <ExpenseList />
        </div>
    );
};

export default HomePage;
